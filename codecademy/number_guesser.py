print 'Welcome to the random number guesser'
print 'Pick a number 1-1000 and keep it in mind'

original_max = 1001
original_min = 0
original_guess = 500

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'

decision = raw_input ('Is your number ' + str(original_guess) + '? (Type "lower"/"higher"/"yes")')
decision.lower()
if decision == 'higher':
  original_min = original_guess
  original_max_plus_min = original_max + original_min
  original_guess = original_max_plus_min / 2
  
elif decision == 'lower':
  original_max = original_guess
  original_max_minus_min = original_max + original_min
  original_guess = original_max_minus_min / 2
  
elif decision == 'yes':
  print 'Ha... Works like a charm'
  quit()
  
else:
  print 'error'